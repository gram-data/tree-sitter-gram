---
alwaysApply: true
---

# tree-sitter-gram Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-26

## Active Technologies
- JavaScript (Node.js) for grammar definition, generated C code for parser + tree-sitter (CLI and runtime), tree-sitter CLI for code generation (001-refactor-terminology)
- N/A (parser generation, no data storage) (001-refactor-terminology)
- JavaScript (Node.js), tree-sitter grammar DSL + ree-sitter-cli (project version), tree-sitter (bindings) (003-extended-annotation)
- N/A (parser grammar definition) (003-extended-annotation)
- Tree-sitter query language (scheme-like .scm); grammar.js (JavaScript, Node); parser generated to C via tree-sitter-cli + ree-sitter, tree-sitter-cli (npm); editors consume queries (Zed, Neovim, Helix, Emacs) (001-editor-improvements)
- N/A (query and doc files only) (001-editor-improvements)
- JavaScript grammar DSL (`grammar.js`), generated C parser (`src/`), Node.js package tooling + `tree-sitter`, `tree-sitter-cli`, Node.js binding toolchain; Tree-sitter queries for editor support (005-tighten-quoted-identifiers)
- N/A (grammar, parser artifacts, docs, and tests in-repo) (005-tighten-quoted-identifiers)
- Rust (workspace `edition = "2021"`, align MSRV with existing `tools/gram-lint`, typically Rust 1.74+ unless CI pins higher) + `tower-lsp`, `tokio`, `tree-sitter` 0.25, `tree-sitter-gram` (path `../..` from `tools/lsp`), `clap` for CLI; optional `ropey` or line-index helpers for UTF-16/LSP positions (see research) (006-gram-lsp)
- In-memory workspace index (open buffers + scanned `.gram` files under workspace root); no database (006-gram-lsp)

- JavaScript (Node.js), tree-sitter grammar DSL + ree-sitter-cli ^0.25.10, tree-sitter ^0.25.0 (001-line-comments)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

JavaScript (Node.js), tree-sitter grammar DSL: Follow standard conventions

## Recent Changes
- 006-gram-lsp: Added Rust (workspace `edition = "2021"`, align MSRV with existing `tools/gram-lint`, typically Rust 1.74+ unless CI pins higher) + `tower-lsp`, `tokio`, `tree-sitter` 0.25, `tree-sitter-gram` (path `../..` from `tools/lsp`), `clap` for CLI; optional `ropey` or line-index helpers for UTF-16/LSP positions (see research)
- 005-tighten-quoted-identifiers: Added JavaScript grammar DSL (`grammar.js`), generated C parser (`src/`), Node.js package tooling + `tree-sitter`, `tree-sitter-cli`, Node.js binding toolchain; Tree-sitter queries for editor suppor
- 001-editor-improvements: Added Tree-sitter query language (scheme-like .scm); grammar.js (JavaScript, Node); parser generated to C via tree-sitter-cli + ree-sitter, tree-sitter-cli (npm); editors consume queries (Zed, Neovim, Helix, Emacs)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:
`specs/006-gram-lsp/plan.md` (and sibling `research.md`, `data-model.md`, `quickstart.md`, `contracts/`).
<!-- SPECKIT END -->
