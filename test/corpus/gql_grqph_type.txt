==================
GQL Node Type Definition
:error
==================

(:Document {
  uuid :: string!,
  title :: string
})

---

==================
GQL Relationship Type Definition
:error
==================

(:Form)=[:SECTION { item :: string }]=>(:Chunk)

---

(gram
  (pattern
    (pattern_element (relationship
      (node (labels (label (symbol))))
      (double_right
        (labels (label (symbol)))
        (record (property (symbol) (symbol)))
      )
      (node (labels (label (symbol))))
    ))
  )
)