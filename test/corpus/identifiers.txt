
==================
Identifier using '@'
==================

(@akollegger)
(akollegger@neo4j.com)

---

(source_file
  (pattern (node_pattern (node (attributes (symbol)))))
  (pattern (node_pattern (node (attributes (symbol)))))
)


==================
Identifier using '_' and mixed case
==================

(_0n96pdf6@E)
(Im0_pWk0g4@@)

---

(source_file
  (pattern (node_pattern (node (attributes (symbol)))))
  (pattern (node_pattern (node (attributes (symbol)))))
)


==================
Identifier using numbers
==================

(42)

---

(source_file
  (pattern (node_pattern (node (attributes (symbol)))))
)


==================
Identifier using numbers with units
==================

(12px)

---

(source_file
  (pattern (node_pattern (node (attributes (symbol)))))
)


==================
Identifier using backticks
==================

(`+1`)

---

(source_file
  (pattern (node_pattern (node (attributes (backticked_string)))))
)



==================
Identifier using backticks with escaped backtick
==================

(`escape \` the backtick`)

---

(source_file
  (pattern (node_pattern (node (attributes (backticked_string)))))
)

==================
Identifier using double-quotes
==================

("Hello World!")

---

(source_file
  (pattern (node_pattern (node (attributes (double_quoted_string)))))
)



==================
Identifier using double-quotes with escaped double-quote
==================

("escape \" the double-quote")

---

(source_file
  (pattern (node_pattern (node (attributes (double_quoted_string)))))
)

==================
Identifier pair
==================

(a),(b)

---

(source_file
  (pattern 
    (node_pattern (node (attributes (symbol))))
    (node_pattern (node (attributes (symbol))))
  )
)

