
==================
Identifier using '@'
==================

(@akollegger)
(akollegger@neo4j.com)

---

(gram
  (pattern (path (node (attributes (symbol)))))
  (pattern (path (node (attributes (symbol)))))
)


==================
Identifier using '_' and mixed case
==================

(_0n96pdf6@E)
(Im0_pWk0g4@@)

---

(gram
  (pattern (path (node (attributes (symbol)))))
  (pattern (path (node (attributes (symbol)))))
)


==================
Identifier using numbers
==================

(42)

---

(gram
  (pattern (path (node (attributes (symbol)))))
)


==================
Identifier using numbers with units
==================

(12px)

---

(gram
  (pattern (path (node (attributes (symbol)))))
)


==================
Identifier using backticks
==================

(`+1`)

---

(gram
  (pattern (path (node (attributes (backticked_string)))))
)



==================
Identifier using backticks with escaped backtick
==================

(`escape \` the backtick`)

---

(gram
  (pattern (path (node (attributes (backticked_string)))))
)

==================
Identifier using double-quotes
==================

("Hello World!")

---

(gram
  (pattern (path (node (attributes (double_quoted_string)))))
)



==================
Identifier using double-quotes with escaped double-quote
==================

("escape \" the double-quote")

---

(gram
  (pattern (path (node (attributes (double_quoted_string)))))
)

==================
Identifier pair
==================

(a),(b)

---

(gram
  (pattern 
    (path (node (attributes (symbol))))
    (path (node (attributes (symbol))))
  )
)

